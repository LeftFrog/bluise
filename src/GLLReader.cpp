#include <vector>
#include <iostream>
#include "Game.h"

using std::string;

// vector<Game> readGLL() {
//     return vector<Game>();
// }

// istream& operator>>(istream& is, Game& game) {
//     while(!is.eof()) {
//         string line;
//         string name;
//         std::getline(is, line);
//         short pos = line.find('{');
//         if(pos==string::npos) {
//             //
//         }
//         name = line.substr(0, line.length() - 2);
//         std::cout << name;
        
//     }
//     return is;
// }
